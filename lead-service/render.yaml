services:
  - type: web
    name: presfads-lead-api
    env: node
    plan: starter
    region: oregon
    branch: develop
    rootDir: lead-service
    buildCommand: npm ci
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
          fromDatabase:
            name: presfads-db
          property: connectionString
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_MESSAGING_SERVICE_SID
        sync: false
      - key: TWILIO_FROM_NUMBER
        sync: false
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: AWS_REGION
        value: us-west-2
      - key: S3_BUCKET
        sync: false
      - key: PUBLIC_BASE_URL
        value: https://presfads.example.com
      - key: TURNSTILE_SECRET
        sync: false
      - key: UPLOAD_TOKEN_SECRET
        sync: false
databases:
  - name: presfads-db
    databaseName: presfads
    plan: starter
    region: oregon
